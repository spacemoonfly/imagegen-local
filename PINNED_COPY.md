# imagegen-local

This is a user-scoped pinned copy of the bundled `.system/imagegen` skill.

Why it exists:
- The bundled system skill can be reinstalled or temporarily disappear during Codex App/runtime refreshes.
- This copy lives under `~/.codex/skills/imagegen-local`, outside `.system`, so it is not tied to the system asset refresh path.
- The skill name is intentionally `imagegen-local` to avoid duplicate-name conflicts while the bundled `imagegen` skill is also present.

Source copied from:
- `/Users/robiny/.codex/skills/.system/imagegen`

Created:
- 2026-04-29
